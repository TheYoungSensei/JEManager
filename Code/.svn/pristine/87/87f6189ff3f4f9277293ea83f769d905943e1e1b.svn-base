package persistence.dao;

import biz.User;
import biz.UserDto;

public interface UserDao {


  /**
   * get a user from its pseudo.
   * 
   * @param pseudo the pseudo of the user
   * @return null if the authentification fails
   */
  User getUserByPseudo(String pseudo);

  /**
   * Create function which create a new user into the the data unit.
   * 
   * @param user the user
   * @return the user's id
   */
  int createUser(UserDto user);

  /**
   * Function which retrieves a user's data from the persistence unit.
   * 
   * @param id target user's id
   * @return the target user's data, null otherwise.
   */
  User getUserById(int id);

  // I don't understand the purpose of this one
  // /**
  // * Update procedure for an user.
  // *
  // * @param manager user to update.
  // */
  // void updateUser(User manager);

}
